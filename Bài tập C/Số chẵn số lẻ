#include <stdio.h>

int main() {
    int num;
    
    scanf("%d", &num);

    if (num % 2 == 0) {
        printf("%d là số chẵn.", num);
    } else {
        printf("%d là số lẻ.", num);
    }

    return 0;
}

#include<stdio.h>
#include<math.h>
void Zero.0( int n[], int size){
	int count= 0;
	 for (int i = 0; i < size; i++) {
		if( n[i]==0){
			count++;
		}
	}
	printf("So luong so khong trong mang la: %d ", count);
}
int main(){
 int a[]={1,4 ,-1, 3, 0,0};
    int size = sizeof(a) / sizeof(a[0]);
    Zero(a,size);
    
}
